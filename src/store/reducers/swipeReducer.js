import * as types from '../../actions/actionTypes'
import initialState from './initialState'

export default function swipeReducer=(state = initialState., action){
    switch(type){
        case actions.DELETE_THUMBNAILS:
            return{
                ...state,
                
            }
    }
}